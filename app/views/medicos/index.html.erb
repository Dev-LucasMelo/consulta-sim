<p style="color: green"><%= notice %></p>

<h1>Medicos</h1>

<div id="medicos">
  <% @medicos.each do |medico| %>
    <%= render medico %>
    <p>
      <%= link_to "Show this medico", medico %>
    </p>
  <% end %>
</div>

<%= link_to "Visualizar consultas", controller: 'consulta', action: 'index' %>
<%= link_to "New medico", new_medico_path %>
